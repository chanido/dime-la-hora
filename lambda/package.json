{
  "name": "dime-la-hora",
  "version": "1.0.0",
  "description": "Backend Node.js para una skill de Alexa que dice la hora en castellano.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "check": "node -e \"const skill = require('./index.js'); if (typeof skill.handler !== 'function') { throw new Error('Missing Lambda handler'); }\"",
    "test": "npm run check && node --test"
  },
  "engines": {
    "node": "16.x || 18.x"
  },
  "dependencies": {
    "ask-sdk-core": "^2.14.0",
    "ask-sdk-dynamodb-persistence-adapter": "^2.13.0",
    "ask-sdk-model": "^1.86.0"
  }
}
